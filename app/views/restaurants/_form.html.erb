<%= simple_form_for(@restaurant) do |f| %>
  <%= f.input :name %>
  <%= f.input :address %>
  <%= f.input :category, :collection => %w[chinese japanese belgian french italian] %>
  <%= f.submit %>
<% end %>
