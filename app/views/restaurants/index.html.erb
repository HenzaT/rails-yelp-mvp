<div class="container">
  <h1>Restaurants:</h1>
  <ul>
    <% @restaurants.each do |restaurant| %>
      <li>
      <%= restaurant.name %>
      </li>
    <% end %>
  </ul>
  <div class="container">
    <%= link_to 'Add a restaurant', '/restaurants/new', class: "btn btn-primary"%>
  </div>
</div>
